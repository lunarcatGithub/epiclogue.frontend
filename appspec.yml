version: 0.0
os: linux
files:
        - source: /
          destination: /home/ubuntu/build/
          overwrite: true
hooks:
        AfterInstall:
        - location: ./scripts/after-install.sh
          timeout: 180 # timeout-seconds
          runas: root
        
